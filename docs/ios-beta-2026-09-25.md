# iOS beta 1.0 (7) — September 25, 2026

## Release source

- Web source: `e0432bd4` on `main`, including the latest merged guest and host updates.
- Bundle identifier remains `com.acefayad.yardly`; signing team remains `TX69NKN54N`.
- Built the static web export and copied it with `cap sync ios`. The native `public` directory matches `out` except for Capacitor's generated Cordova shims.
- No remote development server or separate native UI is configured. iOS uses the same responsive components, styles, and backend configuration as the web build.
- Increased the native build number from 6 to 7. Version remains 1.0.
- Declared no non-exempt encryption: the app uses platform HTTPS/authentication mechanisms and has no custom encryption implementation.

## Verification

- Lint, webpack production build, quality audit, and production dependency audit passed (zero vulnerabilities).
- Full browser test rerun: 99 passed, 1 existing skip. The initial run had an authentication-modal timing timeout in the desktop availability test; its targeted rerun and full rerun both passed without code/test changes.
- iOS simulator build, signed device archive, and App Store IPA export succeeded.
- Native visual verification is not yet complete: both available simulator installations/launches were unusually slow. Do not treat browser tests as an on-device walkthrough.
- TestFlight upload, processing, and tester-group status must be confirmed in App Store Connect separately from archive/export success.

This release packages the current website. It does not implement payments, notifications, or other unfinished product features.
